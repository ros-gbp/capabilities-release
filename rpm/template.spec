Name:           ros-lunar-capabilities
Version:        0.2.0
Release:        0%{?dist}
Summary:        ROS capabilities package

Group:          Development/Libraries
License:        BSD
URL:            http://wiki.ros.org/capabilities
Source0:        %{name}-%{version}.tar.gz

Requires:       PyYAML
Requires:       ros-lunar-bondpy
Requires:       ros-lunar-message-runtime
Requires:       ros-lunar-nodelet
Requires:       ros-lunar-roslaunch
Requires:       ros-lunar-rospy
Requires:       ros-lunar-std-msgs
Requires:       ros-lunar-std-srvs
BuildRequires:  python-coverage
BuildRequires:  python-mock
BuildRequires:  python-pep8
BuildRequires:  ros-lunar-catkin
BuildRequires:  ros-lunar-geometry-msgs
BuildRequires:  ros-lunar-message-generation
BuildRequires:  ros-lunar-roslaunch
BuildRequires:  ros-lunar-rospy
BuildRequires:  ros-lunar-rosservice
BuildRequires:  ros-lunar-rostest
BuildRequires:  ros-lunar-std-msgs
BuildRequires:  ros-lunar-std-srvs

%description
Package which implements capabilities, including code to parse capability
interface specs, to parse capability provider specs, and implement the
capability server.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/lunar" \
        -DCMAKE_PREFIX_PATH="/opt/ros/lunar" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/lunar

%changelog
* Mon Feb 27 2017 William Woodall <william@osrfoundation.org> - 0.2.0-0
- Autogenerated by Bloom

